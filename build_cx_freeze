from cx_Freeze import setup, Executable

setup( name = "ventana",
		version = "1.0",
		description = "ventana",
		executables = [Executable("triangulo2.py")],)



in the console 
py executable.py build